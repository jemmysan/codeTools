// 1-  npm install ngx-pagination 
// 2- import {NgxPaginationModule} from 'ngx-pagination';
// Example : <tbody class=" pt-2" *ngFor="let item of listCat | paginate : {itemsPerPage:nbrPage,currentPage:numPage};" >
//          <pagination-controls (pageChange)="numPage = $event"></pagination-controls>

